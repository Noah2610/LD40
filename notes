
Building max width: 32px

Border sections should always have border to the left (in image)

in sections config:
	@biome = [:left_biome,:right_biome]
		or
	@biome = [:biome]

	@border = :left
		or
	@border = :right

	# Order is important, left to right (x)
	@build_levels = [
		{ x: 64, y: 64 },
		{ x: 128, y: 128 }
	]

